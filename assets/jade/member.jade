extends _layout
block var
    - page.name = 'member'
    - page.title = 'MEMBER'
block contents
    div.lines
    section#member(ng-controller="memberController as c")
        h3 MEMBER
        div.inner
            article(ng-repeat="a in c.list")
                h4(ng-bind="a.name")
                // modal?
    div.lines
